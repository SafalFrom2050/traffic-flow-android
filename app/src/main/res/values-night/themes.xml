<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrafficFlow" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/secondary</item>

        <item name="android:colorBackground">@color/background_dark</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/background_dark</item>
        <!-- Customize your theme here. -->

        <item name="colorOnBackground">@color/colorOnBackground_dark</item>
    </style>

    <style name="Theme.TrafficFlow.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorPrimary">@color/background_dark</item>
    </style>

    <style name="Theme.TrafficFlow.TabLayout" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabTextColor">@color/background_light</item>
        <item name="tabIndicatorColor">@color/secondary</item>
        <item name="android:background">@color/background_dark</item>

    </style>

</resources>